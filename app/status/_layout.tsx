import { Stack } from "expo-router";

export default function StatusLayout() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
            }}
        />
    );
}
